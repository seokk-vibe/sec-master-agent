# Core web API framework
fastapi==0.115.6
uvicorn==0.30.6

# HTTP client for vLLM/MCP integrations
httpx==0.27.2

# Data validation / request-response schemas
pydantic==2.10.3

# YAML config loader
PyYAML==6.0.2

# OpenAI SDK (optional — used when llm_caller_type=openai)
openai>=1.0.0
